# After GUI self-host generation

The production GUI generated and finalized `./ARAMF_WORKER/` for the
repository root. Verify reported PASS after generation and the saved project
reopened with project name ARAMF and the repository-root path.
